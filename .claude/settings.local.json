{
  "cleanupPeriodDays": 99999,
  "permissions": {
    "allow": [
      "Bash(Get-ChildItem -Depth 1 -Directory)",
      "Bash(Select-Object Name)",
      "PowerShell(\\(Get-ChildItem -Recurse -File | Measure-Object -Property Length -Sum\\).Sum / 1MB)"
      "Bash(Select-Object -First 50)",
      "PowerShell($files = git diff --cached --name-only; Write-Host \"Files: $\\($files.Count\\)\"; $total = 0; foreach \\($f in $files\\) { if \\(Test-Path $f\\) { $total += \\(Get-Item $f\\).Length } }; Write-Host \"Size: $\\([math]::Round\\($total/1MB, 1\\)\\) MB\")",
    ]
  }
}